apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- release.yaml
namespace: kube-system
configMapGenerator:
- name: cilium-values
  files:
  - values.yaml
generatorOptions:
  disableNameSuffixHash: true
