# Copy to local/cluster.env on each machine. This file contains NO credentials.
# Reserve these addresses in your router. Use wired Ethernet and reliable SSDs.
API_HOST=192.168.50.11
POD_CIDR=10.42.0.0/16
SERVICE_CIDR=10.43.0.0/16
CLUSTER_DNS=10.43.0.10
# Initially k8s1's address; before HA change to a stable external TCP load balancer.
# Keep API_HOST identical in clusters/laptops/settings.yaml and the Cilium release.
